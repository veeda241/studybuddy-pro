PORT=3001
DANGEROUSLY_DISABLE_HOST_CHECK=true
# Leave REACT_APP_API_URL empty in local dev so requests go through the CRA proxy
# (package.json "proxy" → http://localhost:5000). Set it only for production builds.
# REACT_APP_API_URL=http://127.0.0.1:5000
